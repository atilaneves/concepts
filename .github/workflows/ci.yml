name: CI
on: [push, pull_request]

jobs:
  test:
    name: Test
    strategy:
      matrix:
        os: [ubuntu-20.04]
        dc:
          - dmd-2.100.2

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3

      - name: Install D compiler
        uses: dlang-community/setup-dlang@v1.2.1
        with:
          compiler: ${{ matrix.dc }}

      - name: Run tests
        run: ./ci.sh

      - uses: codecov/codecov-action@v3.1.1
